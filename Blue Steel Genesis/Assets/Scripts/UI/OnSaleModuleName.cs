using UnityEngine;

public class OnSaleModuleName : MonoBehaviour
{
    public int NumberModule;

    public void UpdateName(GameModule module){//getComponent чтобы поменять имя модуля

    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
